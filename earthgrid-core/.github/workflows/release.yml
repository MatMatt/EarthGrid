name: Build & Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgdal-dev libgtk-3-dev libayatana-appindicator3-dev libxdo-dev
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        working-directory: earthgrid-core
        run: cargo build --release
      - name: Strip binaries
        run: |
          strip earthgrid-core/target/release/earthgrid-core
          strip earthgrid-core/target/release/earthgrid-tray
      - name: Package
        run: |
          mkdir -p dist
          cp earthgrid-core/target/release/earthgrid-core dist/earthgrid-linux-x86_64
          cp earthgrid-core/target/release/earthgrid-tray dist/earthgrid-tray-linux-x86_64
          cd dist && tar czf earthgrid-linux-x86_64.tar.gz earthgrid-linux-x86_64 earthgrid-tray-linux-x86_64
      - uses: actions/upload-artifact@v4
        with:
          name: linux-x86_64
          path: dist/earthgrid-linux-x86_64.tar.gz

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: brew install gdal
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        working-directory: earthgrid-core
        run: cargo build --release --bin earthgrid-core
        # tray app needs different approach on macOS (no GTK)
      - name: Strip
        run: strip earthgrid-core/target/release/earthgrid-core
      - name: Package
        run: |
          mkdir -p dist
          cp earthgrid-core/target/release/earthgrid-core dist/earthgrid-macos-arm64
          cd dist && tar czf earthgrid-macos-arm64.tar.gz earthgrid-macos-arm64
      - uses: actions/upload-artifact@v4
        with:
          name: macos-arm64
          path: dist/earthgrid-macos-arm64.tar.gz

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install GDAL via vcpkg
        run: |
          vcpkg install gdal:x64-windows
          echo "GDAL_HOME=C:\vcpkg\installed\x64-windows" >> $env:GITHUB_ENV
          echo "GDAL_LIB_DIR=C:\vcpkg\installed\x64-windows\lib" >> $env:GITHUB_ENV
          echo "C:\vcpkg\installed\x64-windows\bin" >> $env:GITHUB_PATH
      - uses: dtolnay/rust-toolchain@stable
      - name: Build
        working-directory: earthgrid-core
        run: cargo build --release --bin earthgrid-core
        env:
          GDAL_LIB_DIR: C:\vcpkg\installed\x64-windows\lib
      - name: Package
        run: |
          mkdir dist
          copy earthgrid-core\target\release\earthgrid-core.exe dist\earthgrid-windows-x86_64.exe
          Compress-Archive -Path dist\earthgrid-windows-x86_64.exe -DestinationPath dist\earthgrid-windows-x86_64.zip
      - uses: actions/upload-artifact@v4
        with:
          name: windows-x86_64
          path: dist/earthgrid-windows-x86_64.zip

  release:
    needs: [build-linux, build-macos, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/linux-x86_64/earthgrid-linux-x86_64.tar.gz
            artifacts/macos-arm64/earthgrid-macos-arm64.tar.gz
            artifacts/windows-x86_64/earthgrid-windows-x86_64.zip
          generate_release_notes: true
